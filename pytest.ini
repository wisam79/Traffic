[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# عرض تفاصيل أكثر
addopts = -v --tb=short

# تشغيل الاختبارات بالترتيب
markers =
    unit: اختبارات وحدات (سريعة)
    integration: اختبارات تكامل (تحتاج نموذج)
    slow: اختبارات بطيئة
